# API Configuration
VITE_API_URL=http://localhost:3000/api

# Environment
VITE_ENV=development

# Mock Data (set to 'true' to use mock data instead of real API)
# In development mode, mock data is used by default
VITE_USE_MOCK=true

